using System.Drawing;

namespace ThSitePlan
{
    public class ThSitePlanContentEngine
    {
        public void Paint(ThSitePlanContentDevice device, Image image)
        {
            device.PaintImage(image);
        }
    }
}
